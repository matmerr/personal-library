
// used by react-router
// a custom created history object allow redirection of users
// from outside react components
import { createBrowserHistory } from 'history';

export const history = createBrowserHistory();